# ── Web framework ────────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20       # needed for UploadFile in FastAPI

# ── HTTP client (Google OAuth) ────────────────────────────────────────────────
httpx==0.28.1

# ── Database ──────────────────────────────────────────────────────────────────
# SQLite is bundled with Python — no extra package needed

# ── Data validation ───────────────────────────────────────────────────────────
pydantic[email]==2.10.5

# ── Image processing ─────────────────────────────────────────────────────────
Pillow==11.1.0
numpy==2.2.2
imagehash==4.3.1               # perceptual hashing (phash)

# ── Face recognition (choose ONE, comment out the other) ─────────────────────
# Option A: face_recognition (simpler install, 128-d embeddings)
face-recognition==1.3.0        # requires cmake and dlib
# Option B: insightface (512-d embeddings, better accuracy, heavier)
# insightface==0.7.3
# onnxruntime==1.20.1

# ── Voice (optional — enable what you install) ────────────────────────────────
# resemblyzer==0.1.4            # lightweight 256-d d-vector
# librosa==0.10.2               # audio analysis (needed for synthetic detection)
# soundfile==0.12.1             # audio I/O

# ── Synthetic voice detection (optional, heavy) ───────────────────────────────
# speechbrain==1.0.2
# torch>=2.0.0
# torchaudio>=2.0.0

# ── AI image detection (optional, downloads ~500MB model on first run) ────────
# transformers==4.48.2
# torch>=2.0.0

# ── Environment / secrets ─────────────────────────────────────────────────────
python-dotenv==1.0.1
